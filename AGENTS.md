# RideWise

Default: **`mobile-app/`** only.

Do not touch **`api/`** or **`landing/`** until the user explicitly commands it.

When working on the Expo app, follow `.cursor/skills/ridewise-mobile/SKILL.md`.
